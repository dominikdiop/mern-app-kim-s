import React from "react";
import ProductList from "../../component/ProductList/ProductList";

const Food = ({ product }) => {
  return (
    <div>
      <ProductList />
    </div>
  );
};

export default Food;
